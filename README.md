# material-agents

A monorepo containing two projects:

Backend -> LLM agents + conversation tracking (tracking user behavior etc) -> coded in Python 
Frontend -> for fancy displaying -> coded in Typescript (React)